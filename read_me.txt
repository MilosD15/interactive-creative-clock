index.html and style.css - already created for you. Didn't play around with them almost at all.
sketch.js - the main file where you will be writing your code. It contains the working code for the interaction I will have in the final version of my clock.
trainingModel.js - the file I used to record the data for training the model and to export that data as well as to train the model and export my custom model.
data.json - the file that contains the data I recorded for training the model.
model folder - contains the files that are exported when you try to save your custom model and they can be used to load the model in the future.
images folder - contains all the images of the poses that I used to train the model.